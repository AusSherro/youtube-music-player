---
phase: 01-core-wrapper-window-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - electron.vite.config.ts
  - electron-builder.yml
  - src/main/index.ts
  - src/main/window.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/index.html
  - .gitignore
  - .npmrc
autonomous: true
requirements:
  - WRAP-01
  - WRAP-02
  - WRAP-03

must_haves:
  truths:
    - "App launches and loads music.youtube.com in a frameless desktop window"
    - "User can sign in to Google and the session persists across app restarts"
    - "Premium users hear full-quality audio (Widevine DRM is active)"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with electron, electron-vite, TypeScript dependencies"
      contains: "electron-vite"
    - path: "src/main/index.ts"
      provides: "Main process entry — app lifecycle, creates window"
      exports: ["app ready handler"]
    - path: "src/main/window.ts"
      provides: "Window creation with WebContentsView for YouTube Music"
      exports: ["createMainWindow"]
    - path: "src/preload/index.ts"
      provides: "Preload script exposing IPC bridge via contextBridge"
      contains: "contextBridge"
    - path: "electron.vite.config.ts"
      provides: "Build config for main/preload/renderer targets"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/window.ts"
      via: "import createMainWindow"
      pattern: "createMainWindow"
    - from: "src/main/window.ts"
      to: "music.youtube.com"
      via: "WebContentsView.webContents.loadURL"
      pattern: "music\\.youtube\\.com"
    - from: "src/preload/index.ts"
      to: "src/main/index.ts"
      via: "ipcRenderer/ipcMain channel bridge"
      pattern: "contextBridge\\.exposeInMainWorld"
---

<objective>
Scaffold the Electron project and create the core wrapper that loads YouTube Music with Google sign-in support and Widevine DRM.

Purpose: Establishes the working app shell — the foundation everything else builds on. After this plan, the app launches, loads YouTube Music, allows sign-in, and plays premium audio.

Output: Running Electron app with WebContentsView embedding music.youtube.com, session persistence, and DRM support.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-wrapper-window-chrome/01-CONTEXT.md
@.planning/phases/01-core-wrapper-window-chrome/01-UI-SPEC.md
@copilot-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Electron project with electron-vite</name>
  <files>package.json, tsconfig.json, tsconfig.node.json, tsconfig.web.json, electron.vite.config.ts, electron-builder.yml, src/renderer/index.html, .gitignore, .npmrc</files>
  <action>Scaffold and configure the project.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.assert(p.name==='ytp'); console.assert(p.devDependencies['electron-vite']); console.assert(p.dependencies['electron-store']); console.log('PASS')"</automated>
  </verify>
  <done>Project scaffolded with all dependencies installed, build config ready, renderer placeholder in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create main process with WebContentsView, Google auth, and Widevine DRM</name>
  <files>src/main/index.ts, src/main/window.ts, src/preload/index.ts, src/preload/index.d.ts</files>
  <action>Create main process, window module, and preload bridge.</action>
  <verify>
    <automated>npx electron-vite build 2>&1 && echo "BUILD PASS" || echo "BUILD FAIL"</automated>
  </verify>
  <done>Main process creates frameless window with WebContentsView loading YouTube Music. Google auth supported via UA spoofing and session persistence. Widevine DRM enabled. IPC bridge exposes window control channels. Build compiles successfully.</done>
</task>

</tasks>

<verification>
1. `npx electron-vite build` completes without errors for all three targets (main, preload, renderer)
2. `npx electron-vite dev` launches the app (manual check — app window appears)
3. YouTube Music loads inside the app window
4. Title bar area (32px) is reserved at the top (even if empty in this plan)
</verification>

<success_criteria>
- Electron app scaffolded with electron-vite, TypeScript, electron-builder config
- Frameless BrowserWindow with WebContentsView loading music.youtube.com
- Google sign-in works (UA spoofing prevents blocking, session persists)
- Widevine DRM active (Premium audio plays)
- Preload script exposes window control IPC channels
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-wrapper-window-chrome/01-01-SUMMARY.md`
</output>
